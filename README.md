# Midterm project
    Astana IT University
    Student name: Yerden Kenzhebayev
    Group: IT2407

# Requirements
Install VS Code and Jupyter, Python modules. Then create the venv environment and install the packages from `requirements.txt`:
```Bash
pip install -r requirements.txt
```

# Setup

Execute the `init.py` to download the latest version of the dataset from Kaggle Hub into `./data/`:

```Bash
python init.py
```

Change the settings if needed in `config.yaml`